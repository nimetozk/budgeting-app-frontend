/**
  A simple box component that is used to wrap other components I created.
 */

import styled from "styled-components";

const Box = styled.div`
  padding: 20px 20px 20px 0px;
`;

export default Box;
